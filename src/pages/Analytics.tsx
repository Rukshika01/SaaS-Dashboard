import { useState } from 'react';
import { Area, AreaChart, Bar, BarChart, CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { Activity, CheckCircle2, Clock, TrendingUp } from 'lucide-react';
import { useFlow } from '../context/FlowContext';
import { Card } from '../components/ui/Card';
import { Tabs } from '../components/ui/Tabs';

const trend = [{ name: 'Mon', tasks: 18, focus: 72 }, { name: 'Tue', tasks: 24, focus: 81 }, { name: 'Wed', tasks: 21, focus: 76 }, { name: 'Thu', tasks: 31, focus: 88 }, { name: 'Fri', tasks: 27, focus: 84 }];
export function Analytics() {
  const { projects, tasks, team } = useFlow(); const [range, setRange] = useState<'Weekly'|'Monthly'|'Yearly'>('Weekly');
  return <div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-3xl font-semibold tracking-tight">Analytics</h2><p className="text-muted">A polished lens into delivery performance and productivity patterns.</p></div><Tabs tabs={['Weekly','Monthly','Yearly']} value={range} onChange={setRange}/></div><section className="grid gap-4 md:grid-cols-4">{[{icon:TrendingUp,label:'Productivity',value:'87%'},{icon:CheckCircle2,label:'Task completion',value:`${tasks.filter(t=>t.completed).length}/${tasks.length}`},{icon:Activity,label:'Project health',value:`${projects.filter(p=>p.status!=='At risk').length}/${projects.length}`},{icon:Clock,label:'Tracked time',value:'342h'}].map(k => <Card key={k.label}><k.icon className="text-flow-600"/><p className="mt-4 text-2xl font-semibold">{k.value}</p><p className="text-sm text-muted">{k.label}</p></Card>)}</section><section className="grid gap-6 lg:grid-cols-12"><Card className="lg:col-span-8"><h3 className="mb-4 text-lg font-semibold">Productivity trend · {range}</h3><div className="h-80"><ResponsiveContainer><AreaChart data={trend}><defs><linearGradient id="a" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="rgb(var(--brand))" stopOpacity={.3}/><stop offset="95%" stopColor="rgb(var(--brand))" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="rgb(var(--border))" strokeDasharray="4 6" vertical={false}/><XAxis dataKey="name" tick={{ fill: 'rgb(var(--muted))' }}/><YAxis tick={{ fill: 'rgb(var(--muted))' }}/><Tooltip contentStyle={{background:'rgb(var(--surface))',border:'1px solid rgb(var(--border))',borderRadius:12}}/><Area dataKey="focus" stroke="rgb(var(--brand))" fill="url(#a)" strokeWidth={3}/></AreaChart></ResponsiveContainer></div></Card><Card className="lg:col-span-4"><h3 className="mb-4 text-lg font-semibold">Project performance</h3><div className="h-80"><ResponsiveContainer><BarChart data={projects}><XAxis dataKey="name" hide/><Tooltip contentStyle={{background:'rgb(var(--surface))',border:'1px solid rgb(var(--border))',borderRadius:12}}/><Bar dataKey="progress" fill="rgb(var(--brand))" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></Card></section><section className="grid gap-6 lg:grid-cols-2"><Card><h3 className="mb-4 text-lg font-semibold">Task completion trends</h3><div className="h-64"><ResponsiveContainer><LineChart data={trend}><XAxis dataKey="name" tick={{ fill:'rgb(var(--muted))' }}/><YAxis tick={{ fill:'rgb(var(--muted))' }}/><Tooltip contentStyle={{background:'rgb(var(--surface))',border:'1px solid rgb(var(--border))',borderRadius:12}}/><Line dataKey="tasks" stroke="rgb(var(--brand))" strokeWidth={3}/></LineChart></ResponsiveContainer></div></Card><Card><h3 className="mb-4 text-lg font-semibold">Team performance</h3>{team.map(m => <div key={m.id} className="mb-3 flex items-center justify-between"><span className="text-sm">{m.name}</span><span className="text-sm font-medium">{m.productivity}%</span></div>)}</Card></section></div>;
}
